<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalMarket">
        <activity
            android:name=".PantallaInvernadero"
            android:exported="false" />
        <activity
            android:name=".PantallaPagoRealizado"
            android:exported="false" />
        <activity
            android:name=".PantallaPago"
            android:exported="false" />
        <activity
            android:name=".PantallaConfiguracion"
            android:exported="false" />
        <activity
            android:name=".PantallaFavorito"
            android:exported="false" /> <!-- 🧺 CESTA -->
        <activity
            android:name=".PantallaCesta"
            android:exported="false" /> <!-- 📦 PRODUCTO -->
        <activity
            android:name=".PantallaProducto"
            android:exported="false" /> <!-- 🏠 CATÁLOGO (MAIN) -->
        <activity
            android:name=".PantallaCatalogo"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>